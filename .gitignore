# Bazel
.bazel-disk-cache/
.bazel-repo-cache/
.bazelisk/
bazel-*

# Python
__pycache__/
*.pyc
.venv/

# Built wheels
dist/

# Runtime state (never committed)
sigbot.yaml
data/
.nix-services

# The finder bot's checkout (its own git repo; see
# github.com/fughilli/marketplace-finder-bot)
marketplace-finder-bot/

# Credentials (SSH keys, API keys)
credentials/

# nix build result symlinks
result
.deps/

# Claude Code local settings
.claude/settings.local.json

# Python build artifacts
client/build/
*.egg-info/
