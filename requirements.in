# Direct dependencies only. Regenerate the lock with:
#   bazel run //:generate_requirements_lock
# Deps are added per milestone (M4 adds playwright — version must match the
# nixpkgs playwright-driver in flake.nix; bump together).
aiohttp
anthropic
apscheduler>=3.10
httpx
open_clip_torch
pillow
pyyaml
selectolax
websockets

# Test-only
pytest
